# Dépendances
node_modules/

# Environnement (secrets) — seuls les .env.example-* sont versionnés
.env
.env.*
!.env.example-hub
!.env.example-rasp

# Données locales de dev (bases SQLite, vidéos)
.data/
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Builds frontend
dist/

# Certs TLS locaux (générés par mkcert pour le dev — contiennent une clé privée)
docker/certs/

# Divers
*.log
.DS_Store
