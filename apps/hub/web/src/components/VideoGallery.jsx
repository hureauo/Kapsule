import React from 'react';

// Stub — implémenté en phase 4
export default function VideoGallery({ eventId }) {
  return <p className="text--muted">Galerie vidéo disponible après push (phase 4).</p>;
}
