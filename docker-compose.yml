services:
  dev:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development
    command: ["npm", "test"]
